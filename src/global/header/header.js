import  React  from 'react';
import './header.css';

export default class Header extends React.Component {
    render() {
        return (
            <header className="app-header">
                Visualise Your Data
                {/* we can create an alternate header component here */}
            </header>
        );
    }
}
